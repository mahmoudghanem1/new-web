apiVersion: apps/v1
kind: Deployment
metadata:
  name: new-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: new-web
  template:
    metadata:
      labels:
        app: new-web
    spec:
      imagePullSecrets:
        - name: dockerhub-secret
      containers:
        - name: new-web
          image: mahmoudghanem/new-web:2856e275451fee45a2039475736b9fc98671b0b7
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://postgres:5432/newwebdb
            - name: SPRING_DATASOURCE_USERNAME
              value: newweb
            - name: SPRING_DATASOURCE_PASSWORD
              value: newwebpass
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
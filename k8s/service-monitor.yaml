apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: new-web-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: new-web
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
